import { StyleSheet, Text, View } from "react-native";
import React from "react";

const DurationPicker = () => {
  return (
    <View>
      <Text>DurationPicker</Text>
    </View>
  );
};

export default DurationPicker;

const styles = StyleSheet.create({});
